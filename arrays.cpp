#include<iostream>

using namespace std;

int main(){
    // initializing array with 0 for all indexes
    int a[10] = {0};

    // printing array
    for(int i=0; i<10; i++){
        cout<<a[i]<<" ";
    }

    return 0;
}